---
import Layout from '../../layouts/Layout.astro';
import PageWrapper from '../../PageWrapper';
import BrandArchitecturePage from '../../pages-content/services/BrandArchitecture';

const currentPath = Astro.url.pathname;
---

<Layout title="Brand Architecture - Big Invisible" description="Comprehensive brand architecture and structure." keywords="kitsap, poulsbo,seattle, washington, brand architecture, brand structure, brand hierarchy">
	<PageWrapper client:load currentPath={currentPath}>
		<BrandArchitecturePage client:visible currentPath={currentPath} />
	</PageWrapper>
</Layout>
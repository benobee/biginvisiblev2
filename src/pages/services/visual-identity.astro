---
import Layout from '../../layouts/Layout.astro';
import PageWrapper from '../../PageWrapper';
import VisualIdentityPage from '../../pages-content/services/VisualIdentity';

const currentPath = Astro.url.pathname;
---

<Layout title="Visual Identity - Big Invisible" description="Logo design and visual identity systems that make an impact." keywords="kitsap, poulsbo, seattle, washington, visual identity, logo design, brand identity">
	<PageWrapper client:load currentPath={currentPath}>
		<VisualIdentityPage client:visible />
	</PageWrapper>
</Layout>
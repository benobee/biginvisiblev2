---
import Layout from '../../layouts/Layout.astro';
import PageWrapper from '../../PageWrapper';
import BrandStrategyPage from '../../pages-content/services/BrandStrategy';

const currentPath = Astro.url.pathname;
---

<Layout title="Brand Strategy - Big Invisible" description="Strategic brand development and positioning services." keywords="kitsap, poulsbo,seattle, washington, brand strategy, brand positioning, brand development">
	<PageWrapper client:load currentPath={currentPath}>
		<BrandStrategyPage client:visible currentPath={currentPath} />
	</PageWrapper>
</Layout>